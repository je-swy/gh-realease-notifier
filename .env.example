PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=notifier
DB_USER=postgres
DB_PASSWORD=postgres

GITHUB_TOKEN=

# SMTP settings
# For Gmail: use App Password (https://myaccount.google.com/apppasswords)
# For Resend: use smtp.resend.com, port 465, your API key as password
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=your_resend_api_key

BASE_URL=http://localhost:3000